// Firestore security rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write only while authenticated or in dev
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

